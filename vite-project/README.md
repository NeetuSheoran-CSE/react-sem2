# notes

## hooks

1. hooks are functions
2. hooks allowa to use state and other features wiithout writing a class.

>rules to use Hooks
1. only call Hooks at the top level:
2. only call Hooks from React functions:

> react component can't re-render the normal defined variables
